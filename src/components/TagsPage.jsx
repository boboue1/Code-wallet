import styled from 'styled-components';

const PageContainer = styled.div`
`;

function TagsPage() {
  return (
    <PageContainer>
      <p>Tag your snippets to organize them by category.</p>
      {/* À venir : Liste de tags, ajout, édition, suppression... */}
    </PageContainer>
  );
}

export default TagsPage;
